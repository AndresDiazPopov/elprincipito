<!DOCTYPE html>
<html>
<head>
  <%= display_meta_tags :site => ENV['APP_NAME'], 
    :author => '',
    :publisher => '',
    :og => {
      :site_name => ENV['APP_NAME'], 
      :type => 'product', 
      :title => :title, 
      :description => :description,
      :image => image_url('Logo.png')} %>
  <meta charset="UTF-8">
  <title>El Principito</title>
  <!-- Meta etiqueta para la descripcion de la web que se va a ver en el buscador -->
  <meta name="description" content="La mejor web para leer El Principito, de Antoine de Saint-Exupéry, donde quieras. Se adapta a cualquier dispositivo."/>
  <meta name="keywords" content="El Principito, Antoine de Saint-Exupéry, libro">
  <!-- Esta linea es para ordenarle al navegador que se dapte al ancho de la pantalla -->
  <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <%= favicon_link_tag 'favicon.ico' %>
  <!-- Main css styles -->
  <%= stylesheet_link_tag    'public/application', media: 'all', 'data-turbolinks-track' => true %>

  <!-- Main javascript files -->
  <%= javascript_include_tag 'public/application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <%= analytics_init :local => Rails.env.develpoment? %>

  <%= yield :head %>
  
</head>
<body>

  <%= render 'layouts/public/topnavbar' %>

  <%= yield %>


  <footer class="module bg-light">
    <%= render 'layouts/public/footer' %>
  </footer>

</body>
</html>